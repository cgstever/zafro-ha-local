# Minimal local MQTT broker for the bridge + Home Assistant.
# If your broker already exists (e.g. the HA Mosquitto add-on), skip this and
# just point MQTT_HOST/MQTT_PORT at it.
listener 1883 127.0.0.1
allow_anonymous true
